# TODO
- [x] Add a VoiceEnabled checkbox to MainWindow.xaml
- [x] Speak an AI-style startup greeting when the app loads (conditioned on VoiceEnabled)
- [ ] Ensure greeting text is appended to chat and optionally spoken (already spoken; appended only text messages already exist)
- [ ] Build and run to verify voice greeting works

